<?php
$this->layout()->title = $this->translate('Vragen overzicht');
$db = Zend_Registry::get('db');
$items = $this->items;
?>
<section class=categories>

    <form action="vragen/list" method="GET">

        <input type="text" class="text-input" style="width: 50%" name="s" placeholder="Zoek op vragen" />&nbsp;
        <input type="submit" class="button" value="Zoeken" />

    </form>

    <?php if( items ): ?>

    <table>
        <thead>
            <tr>
                <th><?php echo $this->translate('Vraag');?>
                <th>Wijzig
                <th>Verwijder
        </thead>
        <tbody>
            <?php 
            foreach( $items as $i => $item ):
            ?>
            <tr>
                <td><?php echo $item['name']; ?></td>
                <td><a href="<?php echo $this->url(array('action' => 'edit', 'id' => $item['id'])) ?>"><?php echo $this->translate('Wijzig');?></a></td>
                <td class=delete><a href="<?php echo $this->url(array('action' => 'delete', 'id' => $item['id'])) ?>"><?php echo $this->translate('Verwijder');?></a></td>
            </tr>
            <?php
            endforeach;
            ?>
        </tbody>
    </table>

    <?php else: ?>

     <p>Helaas, geen resultaten gevonden op zoekterm "<?php echo $_GET['s']; ?>"</p>

    <?php endif; ?>

</section>